## This file is auto generated by tfskel: https://github.com/ishuar/tfskel

name: Lint envs/dev/eu-central-1/customer-1 Terraform files
on:
  pull_request:
    branches:
      - main
    paths:
      - 'envs/dev/eu-central-1/customer-1/**'
      - '.github/workflows/customer-1-dev-euc1-lint.yaml'

  ## For manual trigger on github UI
  workflow_dispatch:
    inputs:
      enable_terraform_docs_check:
        required: false
        description: Enable terraform-docs check , applicable for modules only.
        type: boolean
        default: false

permissions:
  pull-requests: write
  contents: read

jobs:
  lint:
    uses: ./.github/workflows/reusable-lint.yaml
    with:
      terraform_files_path: 'envs/dev/eu-central-1/customer-1'
      enable_terraform_docs_check: ${{ inputs.enable_terraform_docs_check || false }} # for module workflows set it to true
